#version 410 core

in vec2 TexCoord;
out vec4 fragColor;

// uniform sampler2D DepthMap;

void main()
{       
    // fragColor = vec4(vec3(texture2D(DepthMap, TexCoord).r * 0.5 + 0.5), 1.0); 
    // fragColor = vec4(255.0);
}
